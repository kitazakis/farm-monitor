# Raspberry Pi Zero 2 W Outdoor Logger Worklog - 2026-05-26

## Scope

Stabilized and diagnosed the Raspberry Pi Zero 2 W outdoor observation setup:

- Camera stack isolation and verification
- Lagoon camera upload path
- BLE ITH-11-B sensor replacement for BME280 Harvest
- Persistent logging and incident collection
- Cron safety limits and process locking

## Current Hardware Findings

- Genuine Raspberry Pi camera is detected and works.
- Detected camera sensor: `ov5647`
- Verified modes include `1296x972` and `2592x1944`.
- Direct tests succeeded:
  - `rpicam-hello --list-cameras`
  - `rpicam-still -n --width 1296 --height 972`
  - Picamera2 capture
- Sony SPRESENSE camera board is not compatible with the Raspberry Pi CSI camera stack.
- CSI-to-HDMI conversion board or cable remains suspect. Replacement board is on order.
- Pi CSI port and genuine camera are currently considered healthy.

## Camera Test Results

Successful test artifacts on the Pi:

- `/tmp/camera_test_1296x972.jpg`
- `/tmp/picam2_test_1296x972.jpg`
- `/home/pi/BME280/photo/000972.jpg`

Important note:

- `vcgencmd get_camera` returned `detected=0` even when the camera worked.
- For this Bookworm/libcamera setup, use `rpicam-hello --list-cameras` and Picamera2 results instead.

## Lagoon Changes

Updated remote file:

- `/home/pi/lagoon.py`

Backup:

- `/home/pi/lagoon.py.bak_20260526_2036`

Main changes:

- Moved Picamera2 import into runtime flow with explicit logging.
- Added `Picamera2.global_camera_info()` validation.
- Added upload timeout:
  - `UPLOAD_TIMEOUT = (10, 30)`
- Added robust exception logging with traceback.
- Hardened camera stop/close cleanup.
- Kept image size at `640x480` for Lagoon upload load reduction.

Observed reason for previous timeout:

- Picamera2 import/initialization can take 40-50 seconds on this Pi.
- Previous `timeout 60` could kill `lagoon.py` before upload completed.

Verified:

- Manual Lagoon run succeeded.
- Cron Lagoon run at `20:55` succeeded.
- Uploaded file observed:
  - `image20260526_205615.png`

## Shutter Changes

Updated remote file:

- `/home/pi/BME280/shutter3.py`

Backup:

- `/home/pi/BME280/shutter3.py.bak_20260526_2047`

Main changes:

- Confirmed capture resolution is `1296x972`.
- Confirmed `stop_preview()` is not present.
- Added lazy Picamera2 import logging.
- Added `Picamera2.global_camera_info()` validation.
- Kept robust stop/close cleanup.

Verified:

- Manual shutter run succeeded.
- `camera.set` updated to `972`.
- Captured file:
  - `/home/pi/BME280/photo/000972.jpg`

## Cron Changes

Backup:

- `/home/pi/logs/root_crontab.backup_20260526_2036`

Current important root cron lines:

```cron
# daily reboot
10 03 * * * /sbin/reboot

# incident snapshots
@reboot /bin/sleep 180; /home/pi/logs/incident_snapshot.sh reboot >> /home/pi/logs/incident_snapshot.log 2>&1
07 * * * * /home/pi/logs/incident_snapshot.sh hourly >> /home/pi/logs/incident_snapshot.log 2>&1

# BME280 sensor disabled; replaced by harvest-ble.service
# 15,45 * * * * timeout 30 python3 /home/pi/bme280_1.py >> /home/pi/logs/bme280.log 2>&1

# camera capture
05,35 5-21 * * * flock -n /tmp/shutter.lock timeout 120 sudo -u pi python3 /home/pi/BME280/shutter3.py >> /home/pi/logs/camera.log 2>&1

# Lagoon upload
25,55 5-20 * * * flock -n /tmp/lagoon.lock timeout 180 sudo -u pi python3 /home/pi/lagoon.py >> /home/pi/logs/lagoon.log 2>&1

# Harvest BLE upload runs continuously via harvest-ble.service
# 0,20,40 * * * * flock -n /tmp/harvest.lock timeout 60 python3 /home/pi/harvest.py >> /home/pi/logs/harvest.log 2>&1

# health check
02,12,22,32,42,52 * * * * /home/pi/logs/healthcheck.sh
```

Notes:

- Camera and Lagoon cron now run Python as `pi`, not root.
- `shutter3.py` timeout increased from `40` to `120`.
- `lagoon.py` timeout increased from `60` to `180`.
- A failed cron edit temporarily emptied root crontab; it was restored from backup and verified.

## Ownership Fixes

Changed ownership back to `pi:pi`:

- `/home/pi/BME280/photo`
- `/home/pi/BME280/photo/camera.set`
- `/home/pi/logs/cam-upload.log`

Reason:

- Root-owned `camera.set` prevented manual `pi` execution from saving the shutter number.

## BLE Harvest

Created and enabled:

- `/home/pi/harvest_ble.py`
- `/etc/systemd/system/harvest-ble.service`

Sensor:

- INKBIRD ITH-11-B
- MAC: `49:25:10:25:03:1D`
- Manufacturer data key: `9545`

Verified sample:

- `TEMP=23.8`
- `HUM=59.0`
- `BATT=100`
- `RSSI=-43`
- SORACOM Harvest response: HTTP `201`

CSV backup:

- `/home/pi/logs/ith11b_20260526.csv`

## Logging and Incident Capture

Added or improved:

- `/home/pi/logs/healthcheck.sh`
- `/home/pi/logs/incident_snapshot.sh`
- `/home/pi/logs/incident_snapshots/`
- `/etc/systemd/journald.conf`
  - `Storage=persistent`

Purpose:

- Preserve post-reboot and incident evidence.
- Make `journalctl -b -1` usable after abnormal reboot.

## File Organization

Archived unused files under:

- `/home/pi/archive_20260526_maintenance/`

Important note:

- `lagoon.py` is not upload-only. It also opens Picamera2 and captures an image before upload.

## Current State

- Raspberry Pi was safely shut down after tests.
- Camera stack works with the genuine Raspberry Pi camera.
- SPRESENSE camera should not be used on the Raspberry Pi CSI port.
- CSI-to-HDMI conversion board remains unverified until replacement arrives.
- BLE Harvest service is configured for continuous operation.
- Camera/Lagoon cron jobs are configured with longer timeouts and `flock`.
