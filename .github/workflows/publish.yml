name: Publish GitHub Pages (legacy)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - name: Explain current Pages publishing
        run: |
          echo "GitHub Pages is now published from the main branch /docs folder."
          echo "The sync-data-to-main workflow copies data into docs/data; GitHub Pages publishes that branch source directly."
